# P0.2.1 — Audit du plan d'exécution

**Auditeur :** Claude (orchestrateur)
**Date :** 2026-05-08
**Plan audité :** `docs/sprints/P0.2.1_plan.md` (commit `f3134d2`)
**Auteur du plan :** Codex
**Branche :** `p0-2-1-fix-rls-recursion`

---

## Verdict : ✅ GO

Le plan est conforme au brief P0.2.1, à la recommandation de fix du rapport P0.2, et au protocole standard plan → audit → code → rapport. Aucune correction demandée. Codex peut implémenter la migration immédiatement.

---

## Points forts du plan

1. **Compréhension du bug correcte.** Codex a bien identifié la mécanique : INVOKER + lecture de `profiles` depuis policies de `profiles` ⇒ récursion. Cohérent avec les 3 sources d'évidence du rapport P0.2.

2. **Réutilisation de `current_role_app()` au lieu du sous-`SELECT`.** C'était la simplification suggérée dans le rapport (note de simplification). Codex l'a captée et inscrite explicitement. Bonne lecture.

3. **Honnêteté sur les outils manquants.** Codex marque clairement HANDOFF Claude pour les critères 2, 5, 6, 7 plutôt que de simuler un accès qu'il n'a pas. C'est exactement le comportement anti-hallucination demandé dans le brief.

4. **Critères 1, 3, 4, 8 outillés correctement.** `git diff --stat` pour 1 et 8, Node `fetch` avec publishable key + JWT user pour 3 et 4. Méthode propre et reproductible.

5. **Sacred files cités fidèlement.** Liste reprise verbatim du brief, aucune dérive.

6. **Sémantique préservée explicitement.** Le plan rappelle que la sémantique reste identique (claim `active_establishment_id` prioritaire pour superadmin, fallback `profiles.establishment_id`). Aucune élévation de privilège, conforme au brief.

---

## Points à confirmer avant code (pas bloquants, juste pour ton info)

### Précision sur le timestamp de la migration

Codex écrit : « Le timestamp sera généré au moment de l'implémentation, au format Supabase `YYYYMMDDHHMMSS`. »

**Format attendu** : 14 chiffres exactement, en UTC, postérieur à `20260506105219` (la migration init). Codex peut générer via `date -u +%Y%m%d%H%M%S` (Linux/Cygwin) ou via `Get-Date -Format "yyyyMMddHHmmss"` (PowerShell).

→ Pas de modification du plan. Juste un rappel pour Codex au moment de créer le fichier.

### Précision sur les critères 3 et 4 (REST avec JWT user)

Codex prévoit Node `fetch` avec publishable key + login Auth. Pour que ce soit reproductible et traçable dans le rapport final, je recommande qu'il fasse :

```bash
# 1. Login Auth (récupérer access_token)
curl -X POST 'https://snxdpmxubgjlfdakmnjk.supabase.co/auth/v1/token?grant_type=password' \
  -H 'apikey: sb_publishable_huJd9p4OQ0qe7eBdM0j63w_US3CKvgR' \
  -H 'Content-Type: application/json' \
  -d '{"email":"test@pfmp-pilot.fr","password":"TestPFMP2026!"}'

# 2. Test critère 3
curl -X GET 'https://snxdpmxubgjlfdakmnjk.supabase.co/rest/v1/profiles?select=*&id=eq.a6fff903-f90a-42ea-8f00-f93d988d6046' \
  -H 'apikey: sb_publishable_huJd9p4OQ0qe7eBdM0j63w_US3CKvgR' \
  -H 'Authorization: Bearer <access_token>'

# 3. Test critère 4
curl -X GET 'https://snxdpmxubgjlfdakmnjk.supabase.co/rest/v1/establishments?select=id,name,active&limit=5' \
  -H 'apikey: sb_publishable_huJd9p4OQ0qe7eBdM0j63w_US3CKvgR' \
  -H 'Authorization: Bearer <access_token>'
```

Codex doit reporter dans le rapport final :
- Status HTTP exact (200 attendu)
- Body de la réponse (au moins le nombre de lignes retournées et l'absence de code 54001)

→ Pas de modification du plan. Juste préciser le format de preuve dans le rapport final.

### Plan d'application en prod

Codex écrit : « je n'ai pas de secret `DATABASE_URL`, pas de `psql`, pas de Supabase MCP. Je prévois donc un HANDOFF Claude pour appliquer la migration en prod, ou pour qu'Adel l'applique. »

**Décision** : Claude appliquera la migration en prod via Supabase MCP `apply_migration` une fois que Codex aura push le fichier de migration. Pas besoin que tu fasses la manipulation toi-même.

→ Pas de modification du plan. Juste clarifier l'enchaînement :

1. Codex code la migration en local et push
2. Codex écrit `docs/sprints/P0.2.1_handoff.md` avec le chemin de la migration
3. Claude (moi) applique via Supabase MCP, fait les critères 5/6/7 via MCP
4. Codex récupère mes résultats et écrit le rapport final
5. Codex ouvre la PR

---

## Risque résiduel : aucun

Codex écrit « Aucun risque additionnel identifié ». Je confirme. Le scope est minimal, la sémantique est préservée, le pattern est éprouvé (3 autres helpers déjà DEFINER avec le même pattern dans la même base), le rollback est trivial (re-déclarer la fonction en INVOKER).

---

## Prochaines étapes pour Codex

1. ✅ Plan auditévaliderecevoir GO (ce document)
2. ⏳ Générer le timestamp UTC `<YYYYMMDDHHMMSS>` au moment du commit
3. ⏳ Créer `supabase/migrations/<timestamp>_fix_current_establishment_id_recursion.sql` avec le contenu prescrit verbatim dans le brief P0.2.1 section "Scope IN"
4. ⏳ Commit + push **uniquement la migration** sur la branche `p0-2-1-fix-rls-recursion`
5. ⏳ Écrire `docs/sprints/P0.2.1_handoff.md` (court : "migration prête dans <chemin>, demande à Claude d'appliquer en prod et de lancer critères 2/5/6/7")
6. ⏳ Commit + push le handoff
7. ⏳ STOP — attendre que Claude exécute les critères MCP
8. ⏳ Récupérer les résultats Claude, écrire `docs/sprints/P0.2.1_rapport.md` avec les 8 critères verts (ou statut précis si l'un échoue)
9. ⏳ Ouvrir la PR `docs+fix(P0.2.1): SECURITY DEFINER sur current_establishment_id`

---

**GO Codex. Tu peux démarrer l'étape 2 (création de la migration).**
