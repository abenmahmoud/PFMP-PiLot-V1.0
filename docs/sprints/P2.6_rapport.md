# P2.6 - Rapport d'execution

## Livrable

Premiere brique du studio conventions numeriques :

- affectation d'un modele de convention a chaque classe ;
- selection automatique du modele de classe lors de la preparation des conventions ;
- interface "Studio conventions par classe" dans `/admin/documents` ;
- creation rapide de modeles de convention en markdown avec champs variables ;
- suppression des faux boutons inactifs papier/ZIP dans la barre d'action.

## Fichiers modifies / crees

- `supabase/migrations/20260515194500_p2_6_class_convention_templates.sql`
- `src/lib/database.types.ts`
- `src/server/documents.functions.ts`
- `src/routes/documents.tsx`
- `docs/sprints/P2.6_plan.md`
- `docs/sprints/P2.6_rapport.md`

## Decisions techniques

- Une table `class_document_template_assignments` porte l'affectation classe -> modele.
- Le modele peut rester global (`establishment_id null`) ou propre a un etablissement.
- La preparation des conventions continue de creer une ligne `documents` par eleve, mais choisit maintenant le template affecte a la classe de la periode.
- Le panneau document garde une UX native : select par classe, creation rapide de modele, badges configure/a configurer.

## Reste a faire

- Import DOCX officiel avec conservation/mapping structurel.
- Generation PDF de la convention reelle.
- Upload papier signe.
- Signature entreprise/parent/etablissement branchee sur P2.4 pour les conventions.
- Portail eleve "j'ai trouve un stage".

## Verifications

- `npm run typecheck` : OK
- `npm run build` : OK

Warnings build existants : chunks volumineux, directives `use client` ignorees par Vite/TanStack, warnings CSS `var(...)` deja presents.
